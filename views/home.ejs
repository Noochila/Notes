<%- include("partials/header") -%>

<h1>Home</h1>
<p>
    <%= homeContent %>
</p>

<% p.forEach(function(posts){%>
    <h1>
        <%= posts.title %>
    </h1>
    <p>
       
            <%= posts.description.slice(0,100) %> 
           <% if(posts.title!=""){ %>

                <a href="/posts/<%=posts.title %>">read more</a>
            <%} %>
   
    </p>

    <% })%>

<%- include("partials/footer") -%>

